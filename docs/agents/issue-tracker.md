# Issue Tracker

Issues for this App repo are tracked as **local markdown files** under:

```
.scratch/<feature-slug>/
```

Use this layout when `/to-issues` or `/wayfinder` creates work items for App development.
